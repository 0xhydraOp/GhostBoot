## v1.0.2 — Comprehensive Code Review Fixes

17 improvements from full code audit. Fixes include crash guards, robustness improvements, dead code removal, and battery optimizations.

### Native C++ (Zygisk module)
| Fix | File |
|-----|------|
| `ps -A` fallback to `ps -e` for older ROMs | customize.sh |
| `getprop` `\|\| true` guards for `set -eu` safety | customize.sh |
| `st_name` offset bounds check (>1MB heuristic) | property_hook.cpp |
| logcat logging via `__android_log_print` | property_hook.cpp, main.cpp |
| Check `apply_property_hooks` return value | main.cpp |
| Removed dead `g_env` pointer | main.cpp |
| `list()` returns copy instead of dangling reference | config.cpp, ghostboot.hpp |
| `mprotect` failure logging with errno | property_hook.cpp |

### Shell scripts
| Fix | File |
|-----|------|
| Retry loop (5 attempts) for `am startservice` | service.sh |
| `ps -A` fallback `ps -e` for Zygisk detection | customize.sh |

### Kotlin companion app
| Fix | File |
|-----|------|
| Exact package match for banking detection | MainActivity.kt |
| System package blacklist (prevents accidental system hook) | MainActivity.kt |
| Target list write deduplication (no redundant I/O) | GhostBootService.kt |
| Removed dead `SOCKET_PATH` constant | GhostBootService.kt |

### Metadata
| Fix | File |
|-----|------|
| Version bump to v1.0.2 (versionCode 102) | module.prop |
