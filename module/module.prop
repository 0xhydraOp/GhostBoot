id=ghostboot
name=GhostBoot
version=v1.0
versionCode=100
author=GhostBoot
description=Bootloader spoof & root hide via Zygisk. Per-app mount namespace isolation. No Xposed dependency — runs alongside LSPosed. Hides Magisk + LSPosed from target banking apps. Android 10-15.
